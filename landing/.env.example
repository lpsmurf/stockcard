# Upstash Redis (waitlist storage)
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Optional double opt-in email (Resend) — waitlist works without it
RESEND_API_KEY=
# Canonical site URL for metadata/OG/referral links
NEXT_PUBLIC_SITE_URL=
# Link target of the "Try the demo" section
NEXT_PUBLIC_DEMO_URL=
